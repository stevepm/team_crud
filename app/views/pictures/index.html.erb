<h1>Pictures</h1>
<%= link_to('New Picture', action: 'new') %>
<ol>
  <% @pictures.each do |picture| %>
    <li><a href="/pictures/<%= picture.id %>" id="show_<%= picture.id %>"><img class="thumbnail" src="<%= picture.url %>"/></a></li>
  <% end %>
</ol>